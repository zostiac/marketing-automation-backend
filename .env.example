# Server
PORT=3000
NODE_ENV=development

# CORS
CORS_ORIGIN=*

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Add your Railway DATABASE_URL as a Railway environment variable (not in .env)